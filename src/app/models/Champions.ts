export class Champions {
  id: string;
  fullPicture: any;
  textTitle: string;
  constructor(id: string, fullPicture: any, textTitle: string) {
    this.id = id;
    this.fullPicture = fullPicture;
    this.textTitle = textTitle;
  }
}
